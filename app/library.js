//My function to pass all my test case
//My formular to check for prime numbers 
const myFormula = {
	isPrime: function(n) {
		const solveFirst = Math.floor(n / 2);
		const getFirst = 2 * solveFirst;
		const = n - getFirst;
		return firstResult;
	},
	checkPrime: function(n) {
		const solveFirst = Math.floor(n / 2);
		const getFirst = 3 * solveFirst;
		const firstResult = n - getFirst;
		return firstResult;
	}
};